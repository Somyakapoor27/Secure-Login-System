# Secure-Login-System
User registration and login using hashed passwords (bcrypt or Argon2) Basic input validation and protection from SQL injection Session management with logout feature Optional: Two-Factor Authentication (2FA)
